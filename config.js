window.OPE_CONFIG = Object.assign({
  // Leave blank when the frontend and backend are deployed together.
  // Set this to your deployed backend URL only when the API lives on another origin.
  apiBaseUrl: '',
  syncPollIntervalMs: 5000
}, window.OPE_CONFIG || {});
